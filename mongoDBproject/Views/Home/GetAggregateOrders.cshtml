@model IEnumerable<mongoDBproject.Models.OrderGroup>

@{
    ViewData["Title"] = "GetAggregateOrders";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>GetAggregateOrders</h1>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AvgCountOrder)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LastDateOrder)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FieldUser)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AvgCountOrder)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastDateOrder)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FieldUser.Count)
            </td>
            <td>
                @Html.ActionLink("View", "GetAggregateOrder", new { id = item.Id/* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
<a asp-action="Index">Back to Index</a>